import React from 'react';

import { PlaceholderSection } from '@/components/PlaceholderSection';

export default function TransportsScreen() {
  return (
    <PlaceholderSection
      title="Transports"
      description="Transport list and detail UI to follow; navigation is fully wired to this route."
    />
  );
}
